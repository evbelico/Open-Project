version: "3"

services:
  server:
    container_name: server
    restart: always
    build:
      context: server
      dockerfile: Dockerfile
    links:
      - "op_db"
    environment:
      ENV_NAME: dev
      DATABASE_URL: "postgres://madmonsieur:B@b@082020Yaga!@db:5432/open_project"
    ports:
      - 7001:7001
    volumes:
      - .:/app/
      - /app/node_modules
      # - "./app:/server/src/app"
      # - "/server/src/app/node_modules"
  op_db:
    container_name: op_psql
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: "madmonsieur"
      POSTGRES_PASSWORD: "B@b@082020Yaga!"
      POSTGRES_DATABASE: "open_project"
    expose:
      - 5432
    ports:
      - 5432:5432
